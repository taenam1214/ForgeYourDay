import SwiftUI

struct ProfileView: View {
    var body: some View {
        NavigationView {
            Text("User profile will be here.")
                .navigationTitle("Profile")
        }
    }
}

#Preview {
    ProfileView()
} 